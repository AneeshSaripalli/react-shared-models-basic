# CRA src/ external imports

- `client/src/App.tsx` uses a typescript module external to the CRA `/src` folder
- Code builds fine & shows `dt` in console
- Same approach can be used for the node.js server
